function config = custom()
% This is a custom configuration template to help you get started.
% It is better to copy and modify this file, rather than modifying the core
% configuration files themselves.
%
% To run the model with your custom configurations, simply provide the name
% of the function as the last argument when applying the model, e.g.:
% 
%   out = process_image(in, 'rgb', 20, 'custom');
% 

    config = configurations.default;
    
    % Custom Configurations:
    
    % e.g.: config.wave.transform = 'dwt'
    
end