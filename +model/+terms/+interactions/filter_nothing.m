function no_filter = filter_nothing()
%MODEL.TERMS.INTERACTIONS.FILTER_NOTHING
%   Returns a filter defining no interaction between values.
%   This function is simple, yes, but it makes our code mode explicit and
%   easier to read.
    no_filter = 1;
end

